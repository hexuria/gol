name: catalog
on:
  push:
    branches: [main]
    paths: [".github/workflows/catalog.yml"]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/apply_catalog.py
