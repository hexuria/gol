[package]
name = "harness"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
protocol = { path = "../protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
typesafe-sdk = { version = "0.1.2", features = ["blocking"] }
uuid = "1"
