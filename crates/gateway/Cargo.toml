[package]
name = "gateway"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
protocol = { path = "../protocol" }
serde_json = "=1.0.134"
ureq = { version = "2.12", features = ["json"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros", "time"] }
wiremock = "0.6"
