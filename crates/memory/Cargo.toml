[package]
name = "memory"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
harness = { path = "../harness" }
postgres = { version = "0.19", features = ["with-serde_json-1"] }
protocol = { path = "../protocol" }
serde_json = "=1.0.134"
